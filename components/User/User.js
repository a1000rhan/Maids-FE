import React from "react";
import { StyleSheet, Text, View } from "react-native";

const User = () => {
  return (
    <View>
      <Text>I a User</Text>
    </View>
  );
};

export default User;

const styles = StyleSheet.create({});
